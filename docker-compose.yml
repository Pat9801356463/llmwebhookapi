version: '3.8'

services:
  db:
    image: ankane/pgvector:latest  # ✅ pgvector-enabled image
    container_name: policy_llm_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: policy_llm
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
